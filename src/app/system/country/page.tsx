import BasicTables from '@/app/dashboard/(others-pages)/(tables)/basic-tables/page';
import { Api } from '@/helper/api';
import { readData } from '@/helper/axios';

import CountryListPage from './countryList';


function CountryPage() {

  return (
    <CountryListPage/>
  )
}

export default CountryPage
